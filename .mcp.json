{
  "mcpServers": {
    "codex": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "-u",
        "-c",
        "import os, pathlib, runpy; root=(os.environ.get('CLAUDE_PLUGIN_ROOT') or os.environ.get('CLAUDE_PLUGIN_DIR') or os.environ.get('CLAUDE_PLUGIN_PATH') or os.getcwd()); runpy.run_path(str(pathlib.Path(root)/'codex_bridge_mcp.py'), run_name='__main__')"
      ]
    }
  }
}
